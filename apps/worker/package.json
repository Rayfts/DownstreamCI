{
  "name": "@downstreamci/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Confined execution worker and distributed job consumer for DownstreamCI",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {"build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "test": "vitest run --passWithNoTests"},
  "dependencies": {"@downstreamci/core": "workspace:*", "fastify": "^5.6.0"}
}
